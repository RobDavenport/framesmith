// Shared constants for preview/viewport alignment.

// Fractional Y position (0..1) of the preview origin within the viewport.
// This must match any overlay math that converts between screen and world coords.
export const PREVIEW_ORIGIN_Y_FRAC = 0.78 as const;
